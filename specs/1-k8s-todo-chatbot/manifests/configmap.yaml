apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-chatbot-config
  namespace: todo-chatbot
data:
  # Application configuration
  NODE_ENV: "production"
  PYTHON_ENV: "production"
  # Database configuration (will be overridden by secrets if needed)
  DATABASE_URL: ""
  # Port configurations
  FRONTEND_PORT: "3000"
  BACKEND_PORT: "8000"