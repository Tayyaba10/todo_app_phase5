# Secret manifest template (DO NOT COMMIT with actual values)
# Use sealed-secrets or external secret stores in production
apiVersion: v1
kind: Secret
metadata:
  name: todo-chatbot-secrets
  namespace: todo-chatbot
type: Opaque
data:
  # Base64 encoded values (examples only - replace with actual secrets)
  # Use: echo -n "your-secret-value" | base64
  DB_PASSWORD: "REPLACE_WITH_BASE64_ENCODED_VALUE"
  JWT_SECRET: "REPLACE_WITH_BASE64_ENCODED_VALUE"
  BETTER_AUTH_SECRET: "REPLACE_WITH_BASE64_ENCODED_VALUE"